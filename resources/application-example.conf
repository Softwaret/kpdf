# Example conf file, rename to application.conf in order to use it
ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [com.softwaret.kpdf.application.ApplicationKt.main]
  }
}

config {
  "SALT" = "123"
}

jwt {
  "SECRET" = "123"
  "VALIDITY_MS" = "36000000" // 10Hours
  "ISSUER" = "softwaret"
  "REALM" = "softwaret.kpdf"
}
